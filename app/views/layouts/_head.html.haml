.container-fluid
  .container
    %button.btn.btn-navbar{"data-target" => ".nav-collapse", "data-toggle" => "collapse"}
      .icon-bar
      .icon-bar
      .icon-bar
  = link_to t(:title), "#", :class=>"brand pjax"
  .nav-collapse
    %ul.nav.pull-right
      %li= link_to image_tag('book.png', :border => 0), edit_scores_profile_path(current_teacher.current_profile), :class => 'pop-over', 'data-toggle' => 'tooltip', 'data-content' => t(:scores) if current_teacher.current_profile
      -# %li= link_to Qualifyingentity.model_name.human, qualifyingentities_path()
      -# - if current_teacher.profiles
      -#   %li= link_to QualifyingentityTlresult.model_name.human, assign_tlresults_profile_path(current_teacher, current_teacher.current_profile) unless current_teacher.current_profile.nil?
      %li.dropdown
        = link_to "#", :class => "dropdown-toggle", "data-toggle" => "dropdown" do
          = t(:setting)
          %b.caret
        %ul.dropdown-menu
          %li= link_to t(:profiles), profiles_path
          %li= link_to t(:classrooms), classrooms_path
          %li= link_to t(:qualifyingentities), qualifyingentities_path
          -# %li= link_to QualifyingentityTlresult.model_name.human.pluralize, qualifyingentity_tlresult_path
  #main-navigation
    %ul.nav
      %li.dropdown
        = link_to "#", :class => "dropdown-toggle", "data-toggle" => "dropdown" do
          = current_teacher.email
          %b.caret
        %ul.dropdown-menu
          %li= link_to t(:logout), destroy_teacher_session_path, :method => :delete
      %li
        %a.show_hide{:href=>"#"}
          = t(:show_menu_profiles)
