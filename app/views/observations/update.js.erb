close_dialog('observation_edit');
$('#observations_index').html('<%= escape_javascript(render :partial => 'index', :locals => {:profile => @profile, :student => @student} ) %>');
$('#observations_index').find('tr#<%= resource.id %>').animate({ color: "green" });
$("#aux<%= @student.id %> span.aux4").html('<%= escape_javascript(render :partial => 'profiles/student_observations', :locals => {:profile => @profile, :student => @student} ) %>');
