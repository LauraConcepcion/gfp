.page-header
  %h1=t '.title', :default => QualifyingentityTlresult.model_name.human.pluralize
%table.table.table-striped
  %thead
    %tr
      %th= QualifyingentityTlresult.human_attribute_name(:tlresult)
      %th= QualifyingentityTlresult.human_attribute_name(:qualifyungentity)
      %th= QualifyingentityTlresult.human_attribute_name(:percentage)
      %th=t '.actions', :default => t("helpers.actions")
  %tbody
    - collection.each do |resource|
      = content_tag_for(:tr, resource) do
        %td= resource.tlresult.try(:name)
        %td= resource.qualifyingentity.try(:name)
        %td= resource.try(:percentage)
        %td
          = link_to t(:edit), edit_resource_path(resource), :class => 'btn btn-mini'
          = link_to t(:destroy), resource_path(resource), :data => {:confirm => t(:are_you_sure?)}, :method => :delete, :class => 'btn btn-mini btn-danger'
= link_to t(:new), new_resource_path, :class => 'btn btn-primary'


