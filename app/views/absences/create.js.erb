close_dialog('absence_new');
$("#aux_<%= @student.id %>  span.aux2").html('<%= escape_javascript(render :partial => 'profiles/student_absences', :locals => {:profile => @profile, :student => @student} ) %>')
$('#absences_index').html('<%= escape_javascript(render :partial => 'index', :locals => {:profile => @profile, :student => @student} ) %>');
$('#absences_index').find('tr#<%= resource.id %>').animate({ color: "green" });
