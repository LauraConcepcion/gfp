close_dialog('point_new');
$("#aux_<%= @student.id %> span.aux1").html('<%= escape_javascript(render :partial => 'profiles/student_points', :locals => {:profile => @profile, :student => @student} ) %>')
$('#points_index').html('<%= escape_javascript(render :partial => 'index', :locals => {:profile => @profile, :student => @student} ) %>');
$('#points_index').find('tr#<%= resource.id %>').animate({ color: "green" });
